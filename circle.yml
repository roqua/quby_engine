test:
  override:
    - bundle exec rspec spec:
        environment:
          RAILS_ENV: test
          RACK_ENV: test
    - bundle exec guard-jasmine --server-timeout=120 --timeout=60000 --specdoc=failure
    - bundle exec rubocop