%label.main(for="answer_#{question.key}")= raw(question.title)
.description-and-fields
  .description
    - unless question.description.blank?
      :markdown
        #{raw question.description}
  .fields(id="answer_#{question.key}_input")
    = text_area :answer, question.key, :rows => (question.lines || 6), :autocomplete => question.autocomplete, :class => subquestion ? "subinput" : "", :disabled => disabled ? "" : nil
