%h1== Editing #{@questionnaire.key}

- semantic_form_for(@questionnaire, :url => admin_questionnaire_path(@questionnaire)) do |form|
  - form.inputs :name => "General" do
    = form.inputs :key
  = form.inputs :definition
  = form.buttons